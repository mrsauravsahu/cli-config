# cli-config

## Important

- **Do not modify files in `current/`** — this directory is generated by running cli-config (setup/install/configure steps). Edit the source templates in `src/installers/` instead.
